{% extends 'base.html.twig' %}

{% block body %}
    <h1>Movies</h1>
    <ul>
        {% for movie in movies %}
            <li>
                <a href="{{ path('movie_show', {id: movie.id}) }}">{{ movie.name }}</a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}

